import { AttractionInfoDirective } from './info.directive';

describe('AttractionInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new AttractionInfoDirective();
    expect(directive).toBeTruthy();
  });
});
